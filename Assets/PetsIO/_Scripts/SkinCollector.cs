using UnityEngine;

namespace PetsIO._Scripts
{
    public class SkinCollector : MonoBehaviour
    {
        public GameObject[] skins;
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
