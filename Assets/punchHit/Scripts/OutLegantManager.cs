using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class OutLegantManager : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
    void OnCollisionStay(Collision other){
    	//PlayerController.instance.elastickBack();
    	//other.gameObject.GetComponent<Rigidbody>().AddForce(1,100,100);
    	 }
}
